name: Deploy Frontend
description: Build and deploy frontend to Azure Static Web Apps

inputs:
  resource_group:
    description: 'Azure resource group name'
    required: true
  static_web_app_name:
    description: 'Static Web App name'
    required: true
  azure_client_id:
    description: 'Entra ID SPA App Registration client ID'
    required: true
  azure_tenant_id:
    description: 'Entra ID tenant ID'
    required: true
  frontend_url:
    description: 'Frontend URL (used as redirect URI)'
    required: true
  backend_url:
    description: 'Backend API URL'
    required: true
  backend_client_id:
    description: 'Backend app registration client ID (used to build API scope)'
    required: true
  environment:
    description: 'Deployment environment'
    required: true

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install Dependencies
      shell: bash
      working-directory: frontend
      run: npm ci

    - name: Build Frontend
      shell: bash
      working-directory: frontend
      env:
        REACT_APP_AZURE_CLIENT_ID: ${{ inputs.azure_client_id }}
        REACT_APP_AZURE_TENANT_ID: ${{ inputs.azure_tenant_id }}
        REACT_APP_AZURE_REDIRECT_URI: ${{ inputs.frontend_url }}
        REACT_APP_API_BASE_URL: ${{ inputs.backend_url }}
        REACT_APP_API_SCOPE: api://${{ inputs.backend_client_id }}/access_as_user
      run: npm run build

    - name: Get SWA Deployment Token
      id: swa-token
      shell: bash
      run: |
        TOKEN=$(az staticwebapp secrets list \
          --name "${{ inputs.static_web_app_name }}" \
          --resource-group "${{ inputs.resource_group }}" \
          --query "properties.apiKey" -o tsv)
        echo "::add-mask::$TOKEN"
        echo "token=$TOKEN" >> $GITHUB_OUTPUT

    - name: Deploy to Static Web App
      uses: Azure/static-web-apps-deploy@v1
      with:
        azure_static_web_apps_api_token: ${{ steps.swa-token.outputs.token }}
        repo_token: ${{ github.token }}
        action: upload
        app_location: frontend/build
        skip_app_build: true
